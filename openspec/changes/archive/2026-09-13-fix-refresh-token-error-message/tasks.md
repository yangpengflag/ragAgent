## 1. 红灯

- [ ] 1.1 红灯：写测试——刷新链路 5 类失败（缺令牌 / 签名非法 / jti 已撤销 / 账号停用 / 会话纪元落后）返回的 `message` 为刷新专属文案，且 `error_code` 仍为 `unauthorized`
- [ ] 1.2 红灯：写测试——登录失败文案仍为「用户名或密码错误」（不得被本次改动波及）
- [ ] 1.3 红灯：写测试——刷新令牌过期仍返回 `token_expired`（文案与错误码均不变）

## 2. 绿灯

- [ ] 2.1 新增刷新文案常量并在 `refresh()` 的 5 个失败分支启用
- [ ] 2.2 补充/修订既有用例中对旧文案的断言（若有）

## 3. 门禁与收尾

- [ ] 3.1 `uv run pytest` 全绿、`ruff check .` 与 `mypy app` 零错误
- [ ] 3.2 更新 `openspec/specs/authentication/spec.md`（刷新失败的文案要求），归档本 change
