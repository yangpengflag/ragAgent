## 1. 配置、依赖与规格同步

- [ ] 1.1 在 `backend/pyproject.toml` 增加 `pyjwt`、`pwdlib[argon2]`，`uv sync` 并锁定版本
- [ ] 1.2 红灯：写测试——`APP_SECRET_KEY` 缺失或长度不足时应用启动失败，且错误信息包含配置项名
- [ ] 1.3 绿灯：扩展 `Settings`（签名密钥、访问/刷新令牌有效期、刷新 Cookie 名与属性、允许来源、登录限流阈值与窗口、初始管理员用户名/密码），纳入必填校验清单
- [ ] 1.4 更新 `.env.example`：新增全部配置项，注明生成强密钥的方法，以及生产环境必须开启 Cookie `Secure`
- [ ] 1.5 修订 `openspec/project.md`：技术栈认证库改为 `PyJWT` + `pwdlib[argon2]`；权限模型章节明确「本期只落系统角色，库级 ACL 随知识库能力引入」
- [ ] 1.6 红灯：写测试——`GET /api/v1/health` 在**未携带任何令牌**时仍可访问（鉴权不得误伤既有匿名接口）
- [ ] 1.7 红灯：写测试——允许来源的跨源请求响应包含 `Access-Control-Allow-Credentials: true` 且回显来源；非白名单来源不返回该 CORS 头
- [ ] 1.8 绿灯：开启 CORS 凭据支持（与既有「非 debug 下拒绝通配符来源」的守卫保持一致）

## 2. 账号模型与迁移

- [ ] 2.1 红灯：写测试——创建 `Account` 后自动获得主键、创建/更新时间与空软删标记；`username` 重复时抛出可识别的业务异常
- [ ] 2.2 绿灯：实现 `Account` 模型（`username` 唯一、`display_name`、`password_hash`、`is_active`、`system_role`，继承 `BoundedBase`）
- [ ] 2.3 绿灯：生成 Alembic 迁移（建表 + `username` 唯一索引）
- [ ] 2.4 验证：迁移可重复执行且可回滚（沿用既有测试库守卫，不误伤开发库）

## 3. 密码与令牌原语（纯函数，零 I/O）

- [ ] 3.1 红灯：写测试——同一密码两次哈希互不相同、正确密码校验通过、错误密码不通过
- [ ] 3.2 绿灯：实现密码哈希与校验（Argon2id）+ 强度策略校验（长度 12–128、不得与用户名相同），失败给出规则标识
- [ ] 3.3 红灯：写测试——访问令牌四类情形：有效、已过期、签名被篡改、格式非法
- [ ] 3.4 绿灯：实现访问令牌签发与解析（载荷含账号标识、系统角色、`jti`、`iat`、`exp`）
- [ ] 3.5 红灯：写测试——刷新令牌同样四类情形，且能读出 `jti` 与过期时间
- [ ] 3.6 绿灯：实现刷新令牌签发与解析（与访问令牌分属不同类型，互相不可替代）
- [ ] 3.7 重构：令牌参数（算法、有效期、声明名、令牌类型）与密码策略阈值收敛到单一处，避免散落

## 4. 认证服务

- [ ] 4.1 红灯：写测试——登录成功返回访问令牌与账号信息、下发 `HttpOnly` 刷新 Cookie、响应体不含刷新令牌
- [ ] 4.2 绿灯：实现登录（「用户名不存在」与「密码错误」返回同一对外错误）
- [ ] 4.3 红灯：写测试——停用账号与软删账号登录均被拒
- [ ] 4.4 绿灯：实现登录时的账号状态校验
- [ ] 4.5 红灯：写测试——刷新：有效则返回新访问令牌并轮换 Cookie；旧刷新令牌随即失效；无 Cookie 为 401；请求体中的刷新令牌不被采纳
- [ ] 4.6 绿灯：实现刷新（轮换 + 旧 `jti` 拉黑）
- [ ] 4.7 红灯：写测试——登出：原刷新令牌立即失效；重复登出幂等；不影响同账号其他会话
- [ ] 4.8 绿灯：实现登出（拉黑当前 `jti` + 清除 Cookie）
- [ ] 4.9 红灯：写测试——Redis 不可用时：刷新与登出返回 503 `upstream_unavailable`，而登录限流**放行**（两种失败方向）
- [ ] 4.10 绿灯：实现 Redis 失败的两向处理（撤销 fail-closed / 限流 fail-open + 告警日志）
- [ ] 4.11 红灯：写测试——登录限流：达阈值返回 429、窗口内凭据正确也 429、成功登录清零、窗口过后恢复
- [ ] 4.12 绿灯：实现基于 Redis 的登录失败计数（键含用户名与来源地址，阈值与窗口来自配置）
- [ ] 4.13 红灯：写测试——启动引导：空表且已配置则创建 `ADMIN`；表非空则忽略配置；未配置则给出指引；日志不含密码
- [ ] 4.14 绿灯：实现启动阶段的初始管理员引导
- [ ] 4.15 重构：认证服务对 DB 与 Redis 的依赖收敛为可注入接口，单测用内存替身，**不依赖真实 Redis 与 MySQL**

## 5. 鉴权依赖与路由

- [ ] 5.1 红灯：写测试——鉴权依赖：无令牌 401、格式非法 401、签名篡改 401、有效令牌可取得当前账号
- [ ] 5.2 绿灯：实现 `get_current_user`（校验签名与过期后**查库**确认账号存在且启用）
- [ ] 5.3 红灯：写测试——角色不足返回 403 `access_denied`（与 401 区分）；账号被停用后持有效期内令牌访问返回 401
- [ ] 5.4 绿灯：实现 `require_role(...)` 依赖
- [ ] 5.5 红灯：写测试——`GET /api/v1/auth/me`：有效令牌返回当前账号且无密码字段；无效令牌 401
- [ ] 5.6 绿灯：实现 `auth` 路由（login / refresh / logout / me）并挂载；刷新接口校验 `Origin` 属于允许来源
- [ ] 5.7 红灯：写测试——`users` 路由：管理员可创建/列表/查看/改显示名与角色/停用启用/删除/重置密码；非管理员 403；用户名冲突 409；不合规密码 422
- [ ] 5.8 绿灯：实现 `users` 路由
- [ ] 5.9 红灯：写测试——最后一个已启用 `ADMIN` 不可被停用、删除或降级（409），其他情况可以
- [ ] 5.10 绿灯：实现「最后一个管理员」保护
- [ ] 5.11 红灯：写测试——认证事件审计：登录成功/失败、限流触发、刷新失败、登出均产生结构化日志且含 `request_id`，不含密码与令牌原文
- [ ] 5.12 绿灯：实现认证审计日志
- [ ] 5.13 重构：路由层只声明参数与依赖，业务逻辑全部在 `services/`；错误码与 `api-conventions.md` 逐条对齐

## 6. 真实 HTTP 联调与收尾

- [ ] 6.1 确认 MySQL 与 Redis 就绪，启动后端；配置初始管理员后确认首次启动自动创建该账号（日志只含用户名）
- [ ] 6.2 用初始管理员登录：确认真实响应返回访问令牌、下发 `HttpOnly` 刷新 Cookie、响应体不含刷新令牌
- [ ] 6.3 用访问令牌调用 `/api/v1/auth/me`；再用刷新 Cookie 刷新，确认拿到新访问令牌且旧刷新令牌失效
- [ ] 6.4 登出后确认刷新接口返回 401；再次登出确认幂等；确认其他会话不受影响
- [ ] 6.5 连续失败登录达阈值确认真实返回 429，且窗口内凭据正确同样被拒；缩短窗口后确认恢复正常
- [ ] 6.6 复查：`/api/v1/health` 仍匿名可访问；错误码与 `api-conventions.md` 一致；日志中无密码与令牌原文；`pytest` / `ruff` / `mypy` 全绿
- [ ] 6.7 更新 `AGENTS.md`（认证配置与接口说明）与 `.env.example`，提交变更
