## 0. 前置门禁

- [ ] 0.1 确认 `auth-and-users` 已实现：`POST /api/v1/auth/{login,refresh,logout}`、`GET /api/v1/auth/me` 可用，且跨源凭据（`Allow-Credentials` + 回显来源）已生效。未完成前跳过 §5（真机联调），§1–§4 可用 msw 独立推进

## 1. 会话状态与请求层接线

- [ ] 1.1 红灯：写测试——会话状态机：初始为未知态；登录成功后为已登录并持有账号；登出后为匿名
- [ ] 1.2 绿灯：实现会话 store（Zustand）+ 访问令牌仅存内存（不写 `localStorage` / `sessionStorage`）
- [ ] 1.3 红灯：写测试——`login` / `refresh` / `logout` 以携带凭据的方式发送且请求体不含刷新令牌；**认证端点自身的 401 不触发刷新重放**（刷新返回 401 时不递归、不自等待）
- [ ] 1.4 绿灯：给 `apiFetch` 增加 `credentials` 透传；`features/auth/api.ts` 声明四个接口（`login` / `refresh` / `logout` 以不注入令牌、不重放的方式发出）
- [ ] 1.5 红灯：写测试——刷新实现：成功时更新内存中的访问令牌并返回令牌；失败时返回 `null` 且不抛异常
- [ ] 1.6 绿灯：实现刷新实现并接线 `setTokenRefresher`
- [ ] 1.7 红灯：写测试——启动引导四种情形：有效会话 → 已登录；刷新令牌无效 → 匿名且不渲染受保护内容；**服务不可用（非 401）→ 引导失败态且可重试**；刷新成功但当前账号查询 401 → 匿名
- [ ] 1.8 绿灯：实现启动会话引导（`refresh → me` 两步；区分 401 与其它失败）
- [ ] 1.9 红灯：写测试——刷新失败后会话状态为匿名，且后续业务请求不再携带旧访问令牌
- [ ] 1.10 绿灯：把 `setAuthFailureHandler` 的注册从 `AppLayout` **上移到应用级**，接线「清会话 + 跳登录」
- [ ] 1.11 重构：划清 auth 的 api / store / 引导 三层职责边界，避免互相调用形成环

## 2. 登录页

- [ ] 2.1 红灯：写测试——五态：提交中禁止重复提交、凭据错误提示（不区分账号是否存在）、网络/服务端错误可重试、429 限流提示、成功进入应用
- [ ] 2.2 绿灯：实现登录页与表单（用户名、密码、提交、加载与错误展示）
- [ ] 2.3 绿灯：对「响应体不可读」的失败形态做通用兜底（不依赖 `error_code`，覆盖后端 500 缺 CORS 头的情形）
- [ ] 2.4 重构：抽出可复用的表单错误展示组件，登录页只做编排

## 3. 守卫、原目标回跳与外壳账号区

- [ ] 3.1 红灯：写测试——未登录访问受保护路由跳转登录页且不渲染该路由内容；`/login` 与系统状态页在未登录时可正常访问
- [ ] 3.2 绿灯：实现 `RequireAuth` 守卫并按「公开 / 受保护」分组路由表（公开：`/login`、`/`；受保护：其余）
- [ ] 3.3 红灯：写测试——未登录访问受保护路由后完成登录，自动回到原目标
- [ ] 3.4 绿灯：实现原目标回跳（携带并消费目标地址）
- [ ] 3.5 红灯：写测试——已登录时外壳展示当前账号；触发登出后回到登录页且不再携带旧访问令牌；**服务端登出失败时本地仍完成登出**
- [ ] 3.6 绿灯：实现外壳账号区与登出（本地会话先清、服务端失败只告警）
- [ ] 3.7 重构：整理路由表结构与导航配置，使新增业务页面默认处于守卫之内（注意入口是 `main.tsx` 的 `createBrowserRouter`，`AppRoutes.tsx` 仅供测试）
- [ ] 3.8 验证：以**真实刷新实现**复验「401 → 刷新 → 重放一次」链路（替换既有的注入式假 refresher）

## 4. 测试基建与门禁

- [ ] 4.1 扩展 msw handlers：登录成功 / 凭据错误 / 429、刷新成功 / 失效 / 503、登出、当前账号
- [ ] 4.2 验证：`npm run test` 全绿，且**后端未启动**时同样通过
- [ ] 4.3 验证：`npm run lint`、`npm run typecheck`、`npm run build` 均无错误，测试输出无 React 告警

## 5. 真机联调与收尾

- [ ] 5.1 启动后端与前端，用初始管理员登录，确认进入应用并展示账号（确认跨源凭据真实生效、无 CORS 报错）
- [ ] 5.2 整页刷新，确认会话被自动恢复（未跳登录页）
- [ ] 5.3 用管理员账号停用当前账号（本期无账号管理界面，直接调 `users` 接口），刷新页面确认立即失效并回到登录页
- [ ] 5.4 登出后访问受保护路由回到登录页；再用原刷新令牌调用刷新接口确认 401
- [ ] 5.5 复查：错误提示与后端 `error_code` 一致；控制台无告警；刷新令牌不出现在任何脚本可读位置（含 `localStorage` / `sessionStorage` / 内存之外的存储）
- [ ] 5.6 复查 `auth-and-users` 任务 7.7 关于「500 响应缺 CORS 头」的结论，确认前端的通用错误兜底覆盖该情形
- [ ] 5.7 更新 `AGENTS.md` 与 `frontend/README.md`（会话与守卫约定），提交变更
