## 1. 会话状态与请求层接线

- [ ] 1.1 红灯：写测试——会话状态机：初始为未知态；登录成功后为已登录并持有账号；登出后为匿名
- [ ] 1.2 绿灯：实现会话 store（Zustand）+ 访问令牌仅存内存（不写 `localStorage` / `sessionStorage`）
- [ ] 1.3 红灯：写测试——`login` / `refresh` / `logout` 三个接口以携带凭据的方式发送，且请求体中不含刷新令牌
- [ ] 1.4 绿灯：给 `apiFetch` 增加 `credentials` 透传；`features/auth/api.ts` 声明这四个接口（含 `me`）
- [ ] 1.5 红灯：写测试——刷新实现：成功时更新内存中的访问令牌并返回令牌；失败时返回 `null` 且不抛异常
- [ ] 1.6 绿灯：实现刷新实现并接线 `setTokenRefresher`
- [ ] 1.7 红灯：写测试——启动引导：有有效会话时进入已登录态；无/失效会话时进入匿名态，且期间不渲染受保护内容
- [ ] 1.8 绿灯：实现启动会话引导（应用挂载时执行一次，含未知态加载视图）
- [ ] 1.9 重构：划清 auth 的 api / store / 引导 三层职责边界，避免互相调用形成环

## 2. 登录页

- [ ] 2.1 红灯：写测试——四态：提交中禁止重复提交、凭据错误提示（不区分账号是否存在）、网络错误可重试、成功进入应用
- [ ] 2.2 绿灯：实现登录页与表单（用户名、密码、提交、加载与错误展示）
- [ ] 2.3 重构：抽出可复用的表单错误展示组件，登录页只做编排

## 3. 守卫、原目标回跳与外壳账号区

- [ ] 3.1 红灯：写测试——未登录访问受保护路由跳转登录页且不渲染该路由内容；登录页在未登录时可正常访问
- [ ] 3.2 绿灯：实现 `RequireAuth` 守卫并按「公开 / 受保护」分组路由表
- [ ] 3.3 红灯：写测试——未登录访问受保护路由后完成登录，自动回到原目标
- [ ] 3.4 绿灯：实现原目标回跳（携带并消费目标地址）
- [ ] 3.5 红灯：写测试——已登录时外壳展示当前账号；触发登出后回到登录页，且后续请求不再携带旧访问令牌
- [ ] 3.6 绿灯：实现外壳账号区与登出（本地会话先清、服务端失败只告警）
- [ ] 3.7 重构：整理路由表结构与导航配置，使新增业务页面默认处于守卫之内

## 4. 测试基建与门禁

- [ ] 4.1 扩展 msw handlers：登录成功 / 凭据错误 / 限流、刷新成功 / 失效、登出、当前账号
- [ ] 4.2 验证：`npm run test` 全绿，且**后端未启动**时同样通过
- [ ] 4.3 验证：`npm run lint`、`npm run typecheck`、`npm run build` 均无错误，测试输出无 React 告警

## 5. 真机联调与收尾

- [ ] 5.1 启动后端与前端，用初始管理员登录，确认进入应用并展示账号（确认跨源凭据真实生效、无 CORS 报错）
- [ ] 5.2 整页刷新，确认会话被自动恢复（未跳登录页）
- [ ] 5.3 用管理员停用当前账号后刷新页面，确认立即失效并回到登录页
- [ ] 5.4 登出后访问受保护路由回到登录页；再用原刷新令牌调用刷新接口确认 401
- [ ] 5.5 复查：错误提示与后端 `error_code` 一致、控制台无告警、刷新令牌不出现在前端可读位置
- [ ] 5.6 更新 `AGENTS.md` 与 `frontend/README.md`（会话与守卫约定），提交变更
